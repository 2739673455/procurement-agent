# Copy to .env; this local file is ignored by Git.
# Base URL should end in /v1 (or your provider's compatible prefix).
LLM_BASE_URL=
LLM_MODEL=
LLM_API_KEY=

# AGENT_HOST defaults to the local Docker bridge gateway detected by server.py.
AGENT_PORT=8100
ERPNEXT_URL=http://127.0.0.1:8000
ERPNEXT_SITE=development.localhost
